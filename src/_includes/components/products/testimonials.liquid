{% if product.sections.testimonials.size > 0 %}
<section class="pb-5 testimonials-section">
  
  <div class="container reveal-on-scroll text-center">
    <h2 class="text-uppercase font-header gradient-text3 mb-5" style="font-size: 5rem;">
      USED BY PROS
    </h2>
  </div>

  <div class="container reveal-on-scroll px-1 px-sm-5 px-lg-1">
    <div class="testimonial-card-bg">
      <div class="row gy-5 gx-lg-5">
        
        {% for testimonial in product.sections.testimonials %}
        <div class="col-lg-4">
          <div class="testimonial-item">
            
            <div class="avatar-wrapper">
              <img src="{{ testimonial.image | default: '/assets/images/reviewers/default-avatar.jpg' }}" 
                   alt="{{ testimonial.author }}" 
                   class="testimonial-avatar">
            </div>

            <div class="text-content">
              <div class="testimonial-quote">
                "{{ testimonial.quote }}"
              </div>
              
              <div class="mt-auto">
                <div class="testimonial-author">{{ testimonial.author }}</div>
                <div class="testimonial-role">{{ testimonial.role }}</div>
                {% if testimonial.bands %}
                <div class="testimonial-bands">{{ testimonial.bands }}</div>
                {% endif %}
              </div>
            </div>

          </div>
        </div>
        {% endfor %}

      </div>
    </div>
  </div>

</section>
{% endif %}